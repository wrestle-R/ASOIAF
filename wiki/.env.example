# Populated locally by: npx vercel@latest env pull .env.local
BLOB_READ_WRITE_TOKEN=

# Optional local source directory when publishing replacement maps or sigils.
ASOIAF_STATIC_ASSET_ROOT=

# Optional metadata database override for hosted or offline build testing.
ASOIAF_DB_URL=
